[tool:pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = --tb=short -v -m "not autonomous"
pythonpath = .
markers =
    asyncio: marks tests as async
    autonomous: marks tests as autonomous integration tests (require Docker, not run by default) 